spring:
  application:
    name: smart-queue-management

  datasource:
    url: jdbc:mysql://localhost:3306/smart_queue_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true